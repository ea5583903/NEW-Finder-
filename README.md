# NEW-Finder-

## Run

```sh
./run.sh
```

## Build macOS installer

```sh
chmod +x package-macos-pkg.sh
./package-macos-pkg.sh
```

The installer is written to `dist/Mactonish-1.0.0.pkg`. It installs `Mactonish.app` into `/Applications` and requires Java 17 or newer on the target Mac.
